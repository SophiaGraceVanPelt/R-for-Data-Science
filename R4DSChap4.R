1/200*30

(59 + 73+2) /3

sin(pi/2)

x <- 3*4
x

# to tope <- hit 'Alt' and '-' to make <- 

# i_use_snake_case
# otherPeopleUseCamelCase
# some.people.use.periods

this_is_a_really_long_name <- 2.5

# type 'this' and hit tab to auto populate the above name!

this_is_a_really_long_name

# to make the value 3.5 type 'this' and press 'Ctrl" and '↑'

this_is_a_really_long_name <- 3.5

# 4.3 Calling functions
# function_name(arg1 = val1, arg2 = val2, ...)

seq(1,10)

x <- "hello world"

# to avoid having to assign something, then check to make sure it got assigned correctly, just put parantheses around it
# instead of:
y <- seq(1, 10, length.out = 5)
y
# do this:
(y <- seq(1, 10, length.out = 5))

# the envionment tab on the right shows all the objects you've created



